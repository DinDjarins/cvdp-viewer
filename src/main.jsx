import React from 'react'
import ReactDOM from 'react-dom/client'
import CVDPViewer from './CVDPViewer'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
      <CVDPViewer />
    </React.StrictMode>,
)
